// Function Declaration

iṣẹ pataki() -> int {     // Entry point into the program
    sọpe("Mo ki yin gbogbo agbaye");
    pada 0;               // returning 0 indicates a sucessfull running of the program
}

iṣẹ pewa(orukọ : str) {
    sọpe(orukọ);
}

iṣẹ pewa(orukọ : str) {
    jẹki output : str = "Hello, " + orukọ + "!";
    sọpe(output);
}

iṣẹ ropọ(a : int, b : int) -> int {
    sọpe("Add one and two");
    pada a+b;
}

iṣẹ isChar(testChar : char) -> bool {
    pada testChar == '🙇';
}