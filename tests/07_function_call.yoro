iṣẹ pewa(orukọ : str) {
    sọpe("Hello ", orukọ);
}

iṣẹ ropọ(a : int, b: int) -> int {
    pada a+b;
}

iṣẹ pataki() -> int {
    
    pewa("damisi");

    jẹki addition : int = ropọ(3, 5);

    pada 0;
}